<div class="border p-4 flex flex-col  justify-between mt-4">
  <span class="flex flex-col justify-center font-bold">
    <%= user.full_name %>
  </span>
  <div class="divider my-1 "></div>
  <%= tag.span(standup_content, class: "grow whitespace-pre-line") %>

  <%= link_to 'Comment on Yesterday Work', yesterday_work_standup_meeting_path(@standup_meeting) %>

</div>
