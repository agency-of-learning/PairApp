<div class="border p-4 flex flex-col  justify-between mt-4">
  <span class="flex flex-col justify-center font-bold">
    <%= user.full_name %>
  </span>

  <div class="divider my-1 "></div>

  <%= tag.span class: "grow whitespace-pre-line" do %>
    <%= standup_content %>
    <%= render RichTextReactionComponent.new(rich_text_id: standup_content.id) %>
  <% end %>
</div>
