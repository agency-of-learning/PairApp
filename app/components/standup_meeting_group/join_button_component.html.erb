<%= helpers.turbo_frame_tag dom_id(@standup_meeting_group, :join_or_leave) do %>
  <%= button_to 'Join', standup_meeting_group_joins_path(@standup_meeting_group), class: 'btn btn-success' %>
<% end %>
