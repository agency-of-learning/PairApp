<%= render partial: @standup_meeting_group, locals: { 
  standup_meeting_group_user: @standup_meeting_group_user,
  current_user_standup_meeting: @current_user_standup_meeting,
  standup_meeting: @standup_meetings
  } %>