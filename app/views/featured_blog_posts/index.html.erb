<h1 class="text-3xl font-bold text-center mb-4">Featured Agency Blog</h1>

<div class="max-w-prose mx-auto px-2">
  <%= render BlogPosts::ListComponent.new(
    blog_posts: @featured_blog_posts,
    empty_message: "Blog doesn't have any posts yet."
  ) %>
</div>
