<div>
  <h1 class="font-bold text-4xl">StandupMeetingComments#new</h1>
  

  <%= form_with model:[@standup_meeting, @standup_meeting_comment] do |form|  %>

    <%= form.hidden_field :name, value: @section %>
    <%#= form.text_area :body %>
    <%= form.submit class:"btn btn-accent"%>
  <% end %>
  
</div>
<%= params %>